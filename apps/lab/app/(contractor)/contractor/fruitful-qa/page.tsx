export default async function FruitfulControlPage() {
    return (
        <main className="mx-auto max-w-3xl px-4 py-10">
            <h1 className="text-2xl font-bold text-[var(--foreground)]">Fruitful Control</h1>
            <p className="mt-2 text-[var(--foreground-muted)]">
                QA assistant (V1) — placeholder.
            </p>
        </main>
    );
}