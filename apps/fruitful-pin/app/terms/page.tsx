import { PageHeader } from "@/components/PageHeader";

export const metadata = { title: "Terms" };

export default function TermsPage() {
  return <PageHeader eyebrow="Legal" title="Terms placeholder." description="Terms content will be confirmed during the Fruitful Pin inventory before launch." />;
}
