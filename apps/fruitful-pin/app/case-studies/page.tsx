import { PageHeader } from "@/components/PageHeader";

export const metadata = { title: "Case Studies" };

export default function CaseStudiesPage() {
  return <PageHeader eyebrow="Proof" title="Case studies and result stories." description="This page will hold structured proof modules, campaign lessons, and service-fit examples once the migration inventory confirms what should move." />;
}
