// frontend/app/tools/pinterest-potential/page.tsx
export const dynamic = "force-static";

export default function PinterestPotentialPage() {
  return (
    <section className="space-y-3">
      <h1 className="font-heading text-2xl sm:text-3xl text-[#0B132B]">
        Pinterest Potential Calculator (v1 placeholder)
      </h1>
      <p className="text-sm sm:text-base text-[#171A21]/80">
        This is where the calculator flow will live. The layout already uses the minimal FlowHeader so users stay focused on the tool.
      </p>
    </section>
  );
}
